@csrf
@if(isset($product)) @method('PATCH') @endif
<div><label for="name" class="block text-sm font-medium">Product name</label><input id="name" name="name" value="{{ old('name',$product->name ?? '') }}" required maxlength="150" class="mt-2 w-full rounded-lg border border-slate-300 px-3 py-2">@error('name')<p class="mt-2 text-sm text-red-700">{{ $message }}</p>@enderror</div>
@if(isset($product))<div class="mt-5"><label for="category_id" class="block text-sm font-medium">Category</label><select id="category_id" name="category_id" required class="mt-2 w-full rounded-lg border border-slate-300 px-3 py-2">@foreach($categories as $category)<option value="{{ $category->id }}" @selected((int)old('category_id',$product->category_id)===$category->id)>{{ $category->name }}</option>@endforeach</select>@error('category_id')<p class="mt-2 text-sm text-red-700">{{ $message }}</p>@enderror</div>@else<p class="mt-5 text-sm text-slate-600">Category: <span class="font-semibold">{{ $category->name }}</span></p>@endif
<div class="mt-6 flex gap-3"><button class="rounded-lg bg-slate-900 px-4 py-2.5 font-semibold text-white">Save product</button><a href="{{ route('products.index') }}" class="rounded-lg border px-4 py-2.5 font-semibold">Cancel</a></div>
